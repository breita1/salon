<div class="booking">
 
 

<h2>Thank You</h2>
<p>You have just placed a booking</p>

<div id="itemsContainer">


<table id="customers">
 <tr>

 <% @orders.limit(1).order(id: :desc).each do |order| %>
 
 <td>Order reference number: <%= order.id %></td>
 <% end %>
 </tr>
</table>


<table id="customers">
 <% @total = 0 %>
 <% @orderitems.each do |id,quantity| %>
 <tr>
 <% item = Orderitem.find_by_id(id) %>

 <% timx = (item.quantity * item.price)%>
 
 <% @total += timx %>
 <td><%= item.title %></td>
 <td><%= item.quantity %></td>
 <td><%= number_to_currency(item.quantity * item.price, :unit => "€") %></td>

 
  <td>Order Total: &euro; <%= @total %></td>

 </tr>


 </tr>
 <% end %>


 <tr>


 </tr>
</table>
<%= link_to "Go Home", home_path %>

